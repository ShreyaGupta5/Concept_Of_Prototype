import { LoaderCircle, Inbox, X } from 'lucide-react';
export const Button = ({ children, variant = 'primary', className = '', loading, ...props }) => <button className={`button button-${variant} ${className}`} disabled={loading || props.disabled} {...props}>{loading && <LoaderCircle size={17} className="animate-spin"/>}{children}</button>;
export const Badge = ({ children, tone = 'slate' }) => <span className={`badge badge-${tone}`}>{children}</span>;
export const Spinner = ({ label = 'Loading CampusFlow' }) => <div className="grid min-h-52 place-items-center text-muted"><span className="flex items-center gap-2"><LoaderCircle className="animate-spin"/> {label}</span></div>;
export const EmptyState = ({ title, text, action }) => <div className="empty-state"><span className="empty-icon"><Inbox/></span><h3>{title}</h3><p>{text}</p>{action}</div>;
export const Modal = ({ open, onClose, title, children }) => open ? <div className="modal-backdrop" role="presentation" onMouseDown={onClose}><section className="modal-card" role="dialog" aria-modal="true" aria-label={title} onMouseDown={(event) => event.stopPropagation()}><div className="modal-head"><h2>{title}</h2><button aria-label="Close dialog" className="icon-button" onClick={onClose}><X/></button></div>{children}</section></div> : null;
export const Field = ({ label, error, hint, ...props }) => <label className="field"><span>{label}</span>{props.as === 'textarea' ? <textarea {...props} as={undefined}/> : props.as === 'select' ? <select {...props} as={undefined}>{props.children}</select> : <input {...props}/>} {hint && <small>{hint}</small>}{error && <small className="text-red-600">{error}</small>}</label>;
